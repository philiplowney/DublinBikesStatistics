CREATE TABLE STANDDESCRIPTION (ID BIGINT AUTO_INCREMENT NOT NULL, ADDRESS VARCHAR(255) NOT NULL, LATITUDE DOUBLE NOT NULL, LONGITUDE DOUBLE NOT NULL, NAME VARCHAR(255) NOT NULL, NUMBER INTEGER NOT NULL, PRIMARY KEY (ID))
